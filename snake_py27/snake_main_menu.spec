# -*- mode: python -*-
a = Analysis(['snake_main_menu.py'],
             pathex=['C:\\Users\\Paolo\\Python27\\snake_version1.0'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='snake_main_menu.exe',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='snake_main_menu')
